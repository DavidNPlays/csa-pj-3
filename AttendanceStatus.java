/**
 * Method to assist record attendance for a practice through validation 
 * and user input by using enumeration (only P/A)
 * P = present, A = absent
 * Using an enum ensures that only valid attendance statuses can be
 * used in the program, which prevent error and invalid inputs.
 **/
public enum AttendanceStatus {
    P, A
}
